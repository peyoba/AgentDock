# AgentDock Phase 1 Batch 3 Verification

## 范围

第三批任务：

1. 终端优先 Renderer 组件拆分。
2. UI 测试覆盖当前会话详情默认收起。
3. UI 测试覆盖 API 配置按工具类型分组。
4. 主进程内存 session orchestration。

## 验证命令与结果

```bash
npm run workflow:doctor
```

结果：PASS。

```text
PASS required files present
PASS role documents present
PASS PROJECT_PROFILE.md appears filled
PASS Superpowers skills found
PASS markdown code fences balanced
```

```bash
npm run test:workflow
```

结果：PASS，8 passed。

```bash
npm run test
```

结果：PASS，8 test files / 15 tests。

```text
Test Files  8 passed (8)
Tests       15 passed (15)
```

```bash
npm run build
```

结果：PASS；包含 `npm run typecheck`、Vite renderer build、main/preload TypeScript build。

```bash
python3 <key-like-secret-scan>
```

结果：未发现真实 API key。扫描命中项仅为历史需求/mockup 文档中的占位符或脱敏说明，例如 `***`、`...`、`从钥匙串读取`、`Keychain`。

## 安全结论

- Renderer 默认只展示脱敏 key 预览，不展示完整 secret。
- 当前会话详情默认收起；`Keychain 位置` 只在展开后出现。
- API 配置按 Claude / Codex / Gemini / OpenCode / 全部分组。
- 主进程 session service 仅创建内存 session metadata，不启动 PTY、不读取 Keychain、不返回完整 env。

## 未验证项

- 尚未接入真实 `node-pty`。
- 尚未接入真实 macOS Keychain。
- 尚未启动真实 Claude / Codex PTY 会话。

