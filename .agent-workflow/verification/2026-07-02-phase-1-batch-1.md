# AgentDock Phase 1 Batch 1 Verification

## 范围

第一批任务：

1. 建立 Vitest/jsdom/React Testing Library 应用测试框架。
2. 新增共享领域类型与密钥脱敏工具。
3. 新增 Claude / Codex 启动环境生成器，并覆盖 Codex endpoint 隔离。

## 验证命令与结果

```bash
npm run workflow:doctor
```

结果：PASS

```text
PASS required files present
PASS role documents present
PASS PROJECT_PROFILE.md appears filled
PASS Superpowers skills found
PASS markdown code fences balanced
```

```bash
npm run test:workflow
```

结果：PASS，8 passed。

```bash
npm run test
```

结果：PASS，3 test files / 5 tests。

```text
Test Files  3 passed (3)
Tests       5 passed (5)
```

```bash
npm run build
```

结果：PASS；包含 `npm run typecheck`、Vite renderer build、main/preload TypeScript build。

```bash
python3 <key-like-secret-scan>
```

结果：未发现真实 API key。扫描命中项仅为历史需求/mockup 文档中的占位符或脱敏说明，例如 `***`、`...`、`从钥匙串读取`、`Keychain`。

## 安全结论

- 新增测试只使用 `local-development-secret` 这类明显假 secret。
- 新增代码不包含真实 API Key。
- Renderer / IPC 还未进入实现；后续任务必须继续遵守“不返回完整 secret 或完整 env”的约束。
- Codex 启动环境测试已覆盖 endpoint 隔离：`OPENAI_BASE_URL` 来自选定 Profile。

## 未验证项

- 尚未接入真实 `node-pty`。
- 尚未接入真实 macOS Keychain。
- 尚未启动真实 Claude / Codex PTY 会话。

