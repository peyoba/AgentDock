{
  "name": "agentdock",
  "version": "0.1.5",
  "private": true,
  "description": "AgentDock 代理坞：Claude/Codex/Grok 多配置内嵌终端工作台。",
  "main": "dist/main/main.js",
  "type": "module",
  "scripts": {
    "dev": "concurrently -k \"npm:dev:renderer\" \"npm:dev:electron\"",
    "dev:renderer": "vite --host 127.0.0.1 --port 5173",
    "dev:electron": "wait-on http://127.0.0.1:5173 && cross-env VITE_DEV_SERVER_URL=http://127.0.0.1:5173 tsc -p tsconfig.main.json && electron .",
    "build": "npm run typecheck && vite build && tsc -p tsconfig.main.json",
    "typecheck": "tsc --noEmit && tsc -p tsconfig.main.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "workflow:doctor": "python3 scripts/workflow.py doctor",
    "test:workflow": "python3 -m pytest tests/test_workflow_cli.py -v",
    "package:mac": "npm run build && node scripts/package-mac.mjs",
    "package:win": "npm run build && node scripts/package-win.mjs"
  },
  "dependencies": {
    "@xterm/xterm": "^5.5.0",
    "node-pty": "^1.0.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "undici": "^7.28.0"
  },
  "devDependencies": {
    "@electron/packager": "^20.0.1",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@types/node": "^24.0.10",
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "@vitejs/plugin-react": "^4.6.0",
    "concurrently": "^9.2.0",
    "cross-env": "^7.0.3",
    "electron": "^37.2.0",
    "jsdom": "^29.1.1",
    "typescript": "^5.8.3",
    "vite": "^7.0.0",
    "vitest": "^4.1.9",
    "wait-on": "^8.0.3"
  },
  "optionalDependencies": {
    "@cometix/ccline-darwin-arm64": "1.1.2",
    "keytar": "^7.9.0"
  }
}
